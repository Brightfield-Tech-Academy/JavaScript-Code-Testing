describe("power", function() {

    it("raises to n-th power", function() {
      assert.equal(power(2, 3), 8);
    });
  
});

describe("addTwoDigits", function() {

    it("adds two digits", function() {
      assert.equal(addTwoDigits(2, 3), 5);
    });
  
});